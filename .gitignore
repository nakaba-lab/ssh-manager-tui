/target
**/*.rs.bk
*.pdb

# Claude Code: track shared config (agents/, skills/, settings.json) so it
# follows the repo across machines; keep only machine-local overrides out.
/.claude/settings.local.json
